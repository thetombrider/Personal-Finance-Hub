/**
 * Type definitions for authenticated requests
 * 
 * Re-exports the Express type augmentation from express.d.ts
 */

// Import the global type augmentation to ensure it's included
import "../express.d";

// Re-export to make this a module
export { };
